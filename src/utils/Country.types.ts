export type Flag = {
    svg: string
    png: string
}

export type LanguageNames = {
    deu: { common: string };
    fra: { common: string };
    nld: { common: string };
    // Add more language codes and their names as needed
};